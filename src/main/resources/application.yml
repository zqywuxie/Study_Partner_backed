spring:
  application:
    name: user-center
  datasource:
    username: root
    password: 123
    url: jdbc:mysql://localhost:3306/project?useSSL=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  servlet:
    context-path: /api
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false