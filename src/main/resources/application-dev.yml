spring:
  mail:
    # 配置SMTP服务器地址
    host: smtp.163.com
    # 配置端口号465或者25
    port: 25
    # 发送者邮箱（根据自己邮箱填写）
    username: 15182852522@163.com
    #  配置密码，注意是开启POP3/SMTP的授权密码(根据自己的授权密码填写)
    password: NJKSSWOMGMWJYNMD

    properties:
      mail:
        smtp:
          auth: true
          enable: true
          ssl:
            # 设为true时 端口号设为 465 设为false时 端口号设为25
            enable: false
          socketFactoryClass: javax.net.ssl.SSLSocketFactory
        #表示开启 DEBUG 模式，这样，邮件发送过程的日志会在控制台打印出来，方便排查错误
        debug: true
    # 邮箱编码格式
    default-encoding: UTF-8
  application:
    name: user-center
  datasource:
    username: root
    password: wszqy123.
    url: jdbc:mysql://localhost:3306/usercenter?useSSL=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 配置session的存储方式
  session:
    timeout: 86400
    store-type: redis
  redis:
    password: 123456
    port: 6667
    host: localhost
    database: 1
  #  mvc:
  #    pathmatch:
  #      matching-strategy: ant_path_matcher

mybatis-plus:
  configuration:
    #    mybatis-plus 关闭驼峰转换下划线功能
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
# 增加统一前缀/api
server:
  servlet:
    context-path: /api
    session:
      cookie:
        domain: localhost
#        secure: true
        http-only: false
  port: 8081

# 阿里云OSS配置，图片上传使用
aliyun:
  oss:
    file:
      endpoint: oss-cn-chengdu.aliyuncs.com
      keyid: LTAI5tJGHkR15HAtpMxfrm9N
      keysecret: euFVph4QYL92EkGebPwUrWGZeGHgri
      bucketname: wuxie-image



